@inherits RazorForms.Templates.TemplateBase<RazorForms.FormInput>

@if (Model.Options.RemoveWrappers ?? false)
{
	@await Html.PartialAsync("./LabelRaw.cshtml", Model)
}
else
{
	<div class="@GenerateLabelWrapperClasses()">
		@await Html.PartialAsync("./LabelRaw.cshtml", Model)
	</div>
}